{
  "name": "playwright",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "allure": "allure generate ./test-output/allure-results --clean -o ./test-output/allure-report && allure open ./test-output/allure-report",
    "test:headed": "cross-env test_env=prod npx playwright test --grep-invert @wip --headed",
    "test:smoke": "cross-env test_env=prod npx playwright test --grep @smoke --grep-invert @wip",
    "test:regression": "cross-env test_env=prod npx playwright test --grep @regression --grep-invert @wip",
    "test:performance": "k6 run --vus 2 --duration 5s tests/performance/k6_get_user_api_test.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "allure-commandline": "^2.21.0",
    "allure-playwright": "^2.0.0",
    "cross-env": "^7.0.3",
    "dotenv": "^16.0.3",
    "k6": "^0.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.1",
    "@types/node": "^22.13.5"
  }
}
